
<!--<p><%= journey_match.inspect %></p>-->
<p>
  <h6>
    <% if group %>
    <p>you have a match whith a existing group</p>
      there are <%= pluralize(group.users.count, 'person') %> 
      going to <%= group.start_location.address.split(',')[0] %> 
      from <%= group.end_location.address.split(',')[0] %> 
      at <%= group.start_at.strftime('%A, %b %d') %>
    <% else %>
      No group found
    <% end %>
  </h6>
</p>
